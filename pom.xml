<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>kmg.sn.sn001</groupId>
    <artifactId>sn001-marathon-diary</artifactId>
    <version>0.1.0</version>
    <name>sn001-marathon-diary</name>
    <description>マラソン日記</description>

    <properties>
        <java.version>25</java.version>
        <dbflute.version>1.3.1</dbflute.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.dbflute</groupId>
            <artifactId>dbflute-runtime</artifactId>
            <version>${dbflute.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.dbflute</groupId>
                <artifactId>dbflute-maven-plugin</artifactId>
                <version>1.1.0</version>
                <configuration>
                    <dbfluteVersion>${dbflute.version}</dbfluteVersion>
                    <clientProject>marathon</clientProject>
                    <packageBase>kmg.sn.sn001.dbflute</packageBase>
                    <database>postgresql</database>
                    <databaseDriver>org.postgresql.Driver</databaseDriver>
                    <databaseUrl>jdbc:postgresql://localhost:5432/marathon</databaseUrl>
                    <databaseSchema>public</databaseSchema>
                    <databaseUser>marathon</databaseUser>
                    <databasePassword>marathon</databasePassword>
                    <targetLanguage>java</targetLanguage>
                    <targetContainer>spring</targetContainer>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
