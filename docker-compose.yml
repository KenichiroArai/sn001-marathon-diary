services:
  postgres:
    image: postgres:16
    container_name: marathon-diary-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: marathon
      POSTGRES_USER: marathon
      POSTGRES_PASSWORD: marathon
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
